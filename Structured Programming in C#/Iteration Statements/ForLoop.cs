public class ForLoop
{
    public static void ForLoopVariables()
    {
        // variables - scope within the loop
        //for (int i = 0; ; )
        //{
        //    int a = i;
        //    Console.WriteLine(i);
        //}

        // int b = i; // not accessible outside
        // Console.WriteLine(i);

        // condition  
        //for (int i = 0; i < 10 && i > 0; )
        //{

        //}

        //  // step after each cycle
        //for (int i = 0; i < 10; i++)
        //{

        //}

        // while (true)
        //for (; ; )
        //{

        //}
    }
}